# ci-web/k8s-deployment.yaml
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Service
metadata:
  name: ci-web
  namespace: default
spec:
  selector:
    app: ci-web
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ci-web
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ci-web
  template:
    metadata:
      labels:
        app: ci-web
    spec:
      containers:
        - name: ci-web
          image: ghcr.io/gill-sans/ci-api/capit-web:latest
          ports:
            - containerPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ci-web
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  ingressClassName: nginx
  rules:
    - host: capit-dev.mertenshome.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ci-web
                port:
                  number: 80
